{{#each posts as |post| }}
  <a href="#" class="text-decor"><div class="post-cards" data-id="{{post.id}}">  
    <span>{{post.title}}</span>
    <span class="right-title">Posted on {{format_date post.createdAt}}</span>
  </div></a>
{{/each}}

<script src="/js/homepage.js"></script> 
